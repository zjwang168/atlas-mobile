> **DEPRECATED** — This document describes an earlier architecture that used `@gorhom/bottom-sheet`, a `Sidekick.tsx` panel, and a `SearchBar.tsx` inside `src/features/home/`. That design was replaced.
>
> **Read `MY-PLAN.md` instead.** It documents the current `MyPlan` component.
